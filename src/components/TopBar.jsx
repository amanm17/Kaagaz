export default function TopBar({book,page,totalPages,showProgress,setShowProgress,flipMode,setFlipMode,onHome,onOpenBookmarks,onOpenNotes}){const pct=totalPages?Math.round(page/totalPages*100):0;return <header className="topbar"><button className="brand" onClick={onHome}>Kaagaz</button><div className="book-title"><span>{book?.name||'Untitled book'}</span><small>{totalPages?`Page ${page} / ${totalPages}`:'Preparing pages'}</small></div><div className="top-actions">{showProgress&&<span className="progress-pill">{pct}%</span>}<label className="switch"><input type="checkbox" checked={showProgress} onChange={e=>setShowProgress(e.target.checked)}/><span/>Progress</label><label className="switch"><input type="checkbox" checked={flipMode} onChange={e=>setFlipMode(e.target.checked)}/><span/>Page turn</label><button className="ghost-btn" onClick={onOpenBookmarks}>Bookmarks</button><button className="ghost-btn" onClick={onOpenNotes}>Notes</button></div></header>}
