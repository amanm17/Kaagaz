export async function hashFile(file){const max=Math.min(file.size,8*1024*1024);const head=await file.slice(0,max).arrayBuffer();const meta=new TextEncoder().encode(`${file.name}:${file.size}:${file.lastModified}`);const merged=new Uint8Array(head.byteLength+meta.byteLength);merged.set(new Uint8Array(head),0);merged.set(meta,head.byteLength);const digest=await crypto.subtle.digest('SHA-256',merged);return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,'0')).join('')}
